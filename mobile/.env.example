# URL pública da API. Prefixo EXPO_PUBLIC_ é obrigatório para o Expo embutir
# a variável no bundle — nunca coloque segredos aqui, só configuração pública.
EXPO_PUBLIC_API_URL=http://localhost:3001/api
